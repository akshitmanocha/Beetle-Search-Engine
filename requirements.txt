# Beetle Research Sprint — pinned dependencies.
#
# All versions resolve with prebuilt wheels on macOS arm64 / Python 3.9+
# (verified via `pip install --dry-run` on a clean Python 3.9.6 venv) and
# install without an external GPU. torch runs on Apple-Silicon MPS or CPU.
#
# Regenerate the resolved set with:
#   pip install -r requirements.txt && pip freeze

# --- Serving ---
fastapi==0.128.8
uvicorn==0.39.0
pydantic==2.13.4

# --- Retrieval / models ---
faiss-cpu==1.13.0
sentence-transformers==5.1.2
transformers==4.57.6
torch==2.8.0
Whoosh==2.7.4
scikit-learn==1.6.1
numpy==2.0.2

# --- Evaluation harness (sprint additions) ---
# beir pulls in pytrec_eval-terrier + datasets + pandas as transitive deps,
# but they are pinned explicitly here since the eval harness imports them directly.
beir==2.2.0
datasets==4.5.0
pandas==2.3.3
matplotlib==3.9.4

# --- Tests (sprint additions) ---
pytest==8.4.2
hypothesis==6.141.1

# --- ETL / reproducibility (existing pipeline) ---
dvc==3.67.1
beautifulsoup4==4.15.0
trafilatura==2.0.0
readability-lxml==0.8.4.1
PyYAML==6.0.3
